// startup introduction/*var http = require('http');http.createServer(function(req, res) {  res.writeHead(200, {'Content-Type' : 'text/plain'});  res.end('hello world');}).listen(8888);console.log('http server listening at port 8888');*/// @description: Global// process// console -> stdio/** * @description: require is actually not a global but local to each module */// require() -> Modules/** *  @description: Use the internal require() to look up the location of a module, but rather than loading the module, just return the *  resolved filename. */// require.resolve()/** *  @description: An array of search paths for require(). This array can be modified to add custom paths. *  I.e. the path to be search when using require. */// require.paths// Example: add a new path to the beginning of the search list.// require.paths.unshift('/usr/local/node');/** *  @description: __filename *  The filename of the script being executed. This is the absolute path, and not necessarily the same filename passed *  in as a command line argument. */// Example: running node.example.js from /Usr/mjr// console.log(__filename);// // /Usr/mjr/example.jsconsole.log(__filename);// `__filename` isn't actually a global but rather local to each module.//<http://nodejs.org/docs/v0.10.28/api/all.html>// __dirname {String}// module {Object}// export//// @question: When to use exports and when to use module.exports// setTimeout(cb, ms)// Example:console.log(new Date() + ':' + 'Hello ');setTimeout(function() {  console.log(new Date() + ':' + 'world!');}, 1000);// clearTimeout(t)// setInterval(cb, ms);